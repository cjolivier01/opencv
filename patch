--- a/modules/core/src/cuda_info.cpp
+++ b/modules/core/src/cuda_info.cpp
@@ -494,7 +494,9 @@ bool cv::cuda::DeviceInfo::kernelExecTimeoutEnabled() const
 #ifndef HAVE_CUDA
     throw_no_cuda();
 #else
-    return deviceProps().get(device_id_)->kernelExecTimeoutEnabled != 0;
+    int timeout = 0;
+    cudaDeviceGetAttribute(&timeout, cudaDevAttrKernelExecTimeout, device_id_);
+    return timeout != 0;
 #endif
 }
 
@@ -519,17 +521,10 @@ DeviceInfo::ComputeMode cv::cuda::DeviceInfo::computeMode() const
 #ifndef HAVE_CUDA
     throw_no_cuda();
 #else
-    static const ComputeMode tbl[] =
-    {
-        ComputeModeDefault,
-        ComputeModeExclusive,
-        ComputeModeProhibited,
-        ComputeModeExclusiveProcess
-    };
-
-    return tbl[deviceProps().get(device_id_)->computeMode];
+    int mode = 0;
+    cudaDeviceGetAttribute(&mode, cudaDevAttrComputeMode, device_id_);
+    return static_cast<ComputeMode>(mode);
 #endif
 }
 
@@ -561,7 +556,11 @@ int cv::cuda::DeviceInfo::maxTexture1DLinear() const
 #ifndef HAVE_CUDA
     throw_no_cuda();
 #else
-    return deviceProps().get(device_id_)->maxTexture1DLinear;
+    int maxSize = 0;
+    cudaError_t err = cudaDeviceGetAttribute(&maxSize, cudaDevAttrMaxTexture1DLinear, device_id_);
+    if (err != cudaSuccess)
+        return 0;
+    return maxSize;
 #endif
 }
 
@@ -800,7 +799,9 @@ int cv::cuda::DeviceInfo::memoryClockRate() const
 #ifndef HAVE_CUDA
     throw_no_cuda();
 #else
-    return deviceProps().get(device_id_)->memoryClockRate;
+    int clockRate = 0;
+    cudaDeviceGetAttribute(&clockRate, cudaDevAttrMemoryClockRate, device_id_);
+    return clockRate;
 #endif
 }
 
@@ -959,9 +960,15 @@ void cv::cuda::printCudaDeviceInfo(int device)
         printf("  Maximum memory pitch:                          %u bytes\n", (int)prop.memPitch);
         printf("  Texture alignment:                             %u bytes\n", (int)prop.textureAlignment);
 
-        printf("  Concurrent copy and execution:                 %s with %d copy engine(s)\n", (prop.deviceOverlap ? "Yes" : "No"), prop.asyncEngineCount);
-        printf("  Run time limit on kernels:                     %s\n", prop.kernelExecTimeoutEnabled ? "Yes" : "No");
+        bool hasOverlap = prop.asyncEngineCount > 0;
+        printf("  Concurrent copy and execution:                 %s with %d copy engine(s)\n", (hasOverlap ? "Yes" : "No"), prop.asyncEngineCount);
+        
+        int timeout = 0;
+        cudaDeviceGetAttribute(&timeout, cudaDevAttrKernelExecTimeout, dev);
+        printf("  Run time limit on kernels:                     %s\n", timeout ? "Yes" : "No");
+        
         printf("  Integrated GPU sharing Host Memory:            %s\n", prop.integrated ? "Yes" : "No");
         printf("  Support host page-locked memory mapping:       %s\n", prop.canMapHostMemory ? "Yes" : "No");
 
@@ -971,7 +978,10 @@ void cv::cuda::printCudaDeviceInfo(int device)
         printf("  Device is using TCC driver mode:               %s\n", prop.tccDriver ? "Yes" : "No");
         printf("  Device supports Unified Addressing (UVA):      %s\n", prop.unifiedAddressing ? "Yes" : "No");
         printf("  Device PCI Bus ID / PCI location ID:           %d / %d\n", prop.pciBusID, prop.pciDeviceID );
+        
+        int computeModeValue = 0;
+        cudaDeviceGetAttribute(&computeModeValue, cudaDevAttrComputeMode, dev);
         printf("  Compute Mode:\n");
-        printf("      %s \n", computeMode[prop.computeMode]);
+        printf("      %s \n", computeMode[computeModeValue]);
     }